TODO
全てのTagを横一列に並べて、横スクロールできるようにする
ボタンでタグを追加してそれからフォームの中で追加した時に上手くいかない問題を解決する
今までに追加したTagを重複を取り除いて表示する、getのたびにmemoryListにあるtagを重複取り除いて表示させる。 wi
検索フォームをつけるときは全てのmemoryを表示しているとき。復習するときと全てを見るとき用にフィードを切り替えられるようにする。


DONE
Tagがフォームの中にきちんと入るようにする
配列に””が入らないようにする
前回使ったtagをボタンにして表示して選択するとtagの入力欄にタグ表示されるようにする。
reviewメソッドを使って、かくオブジェクトの持つ値を個別に変えらるようにする。
tagがちゃんと追加されるようにする
Tagボタンを押した時に、どんどんtagが追加されていくようにする。


WHY
        return this.memory.tags.filter((x, i, self) => {
          return self.indexOf(x) === i
        })
なぜこれで重複のない配列が返るのか



memo

// tagボタンとtagのフォームを一つのクラスで囲って、
フォームをrelative、ボタンをabsoluteにしてやれば
ボタンがいつもフォームのすぐ下に来るようになれる。

Problem
日本語をtagに入力した時に表示がおかしくなる→widthが0pxになっているからだけどなんで英語は大丈夫なんだ
半角スペース、全角スペース両方でtagを区切ることができない

idea
右のところにONENOTEの左にあるようなやつを作って、それにチェックボックスを作ってそれがオンになったら
そのページの言葉で検索するっていう機能をcomputedで作ろう。便利だと思う。
デフォルトではfalseにしておいてチェックがついたらonにしてonになったらその言葉絞り込む。
これは必然的にタグ検索になるな。本文検索も別に用意する必要があるがメインに使われるのはタグ検索になるだろう。




今までは、firebaseを使っていたがDBとしてしか使っていなかったのか。おそらくfirebaseはdelelteメソッドを
他のHTTPクライアントからできなくしている。











