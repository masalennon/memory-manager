TODO
全てのTagを横一列に並べて、横スクロールできるようにする
ボタンでタグを追加してそれからフォームの中で追加した時に上手くいかない問題を解決する
今までに追加したTagを重複を取り除いて表示する

DONE
Tagがフォームの中にきちんと入るようにする
配列に””が入らないようにする
前回使ったtagをボタンにして表示して選択するとtagの入力欄にタグ表示されるようにする。
reviewメソッドを使って、かくオブジェクトの持つ値を個別に変えらるようにする。
tagがちゃんと追加されるようにする
Tagボタンを押した時に、どんどんtagが追加されていくようにする。


WHY
        return this.memory.tags.filter((x, i, self) => {
          return self.indexOf(x) === i
        })
なぜこれで重複のない配列が返るのか



memo

// tagボタンとtagのフォームを一つのクラスで囲って、
フォームをrelative、ボタンをabsoluteにしてやれば
ボタンがいつもフォームのすぐ下に来るようになれる。

Problem
日本語をtagに入力した時に表示がおかしくなる→widthが0pxになっているからだけどなんで英語は大丈夫なんだ
半角スペース、全角スペース両方でtagを区切ることができない
